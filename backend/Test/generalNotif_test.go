package Test

import "testing"

func TestGeneralNotif(t *testing.T) {
	t.Run("TestGeneralNotif", func(t *testing.T) {
		got := ""
		want := ""
		if got != want {
			t.Errorf("got %q want %q", got, want)
		}
	})
}
